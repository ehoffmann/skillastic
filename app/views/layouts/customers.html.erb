<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Skillastic - Customer" %></title>
    <%= render "layouts/head" %>
  </head>
  <body>

    <div class="ui container">
      <%= render "layouts/customers_nav" %>
      <%= render "layouts/messages"   %>
      <%= yield %>
    </div>

  </body>
</html>
